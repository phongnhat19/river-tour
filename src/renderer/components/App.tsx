import {hot} from "react-hot-loader/root";
import React from "react";

export default hot((): JSX.Element => 
    (
        <div>
          <h1>
            Your Electron App
          </h1>
        </div>
    ));